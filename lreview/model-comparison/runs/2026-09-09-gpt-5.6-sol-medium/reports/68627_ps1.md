# 68627 ps1 — LU-20689 utils: Support decimal (SI) size units

- **Change:** https://review.whamcloud.com/c/fs/lustre-release/+/68627 (patchset 1, `c756d1ed0844`)
- **Review:** 2 finding(s), severity **low**
- **Run:** gpt-5.6-sol, 3.1M tokens, 11m03s

## Overall assessment

Mostly good, with minor documentation comments inline.

## Findings

### 1. `lustre/utils/liblustreapi.c` (line 270)

(minor) Since this changes the contract of the public `llapi_parse_size()` API, could the patch also add an `llapi_parse_size(3)` page and register it in `Documentation/man3/Makefile.am`? The in-source kernel-doc is not installed for liblustreapi consumers.

### 2. `lustre/utils/lhsmtool_posix.c` (line 227)

(minor) Could this spell out the accepted suffixes and the new distinction between `M`/`MiB` and `MB`? This is the copytool's user-facing help, but "unit can be used" does not document that `MB` now selects decimal units.
